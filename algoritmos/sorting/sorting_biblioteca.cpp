#include <iostream>
#include "sorting_biblioteca.h"
#include <algorithm>

void sortingBiblioteca(std::vector<int32_t> &arr)
{
    // Utiliza std::sort de la STL de C++ para ordenar el vector in-place
    std::sort(arr.begin(), arr.end());
}